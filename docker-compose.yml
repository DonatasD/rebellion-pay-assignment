version: "3"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:3000
    environment:
      - PORT=3000
#      - CLOUDINARY_CLOUD_NAME=
#      - CLOUDINARY_API_KEY=
#      - CLOUDINARY_API_SECRET=
